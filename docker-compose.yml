version: "3.7"
services:
  app:
    build:
      context: .
      # target: build
    volumes:
      - "./config.yml:/app/config.yml"
    environment:
      DEBUG: "${DEBUG:-false}"
    ports:
      - "${APP_PORT:-8000}:8000"
